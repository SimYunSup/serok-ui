---
title: Checkbox 예시
description: 체크박스 컴포넌트의 다양한 사용 예시
---

## 기본 사용법

Checkbox 컴포넌트의 가장 간단한 사용 방법입니다.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function BasicExample() {
  return <Checkbox variant="default">약관에 동의합니다</Checkbox>;
}
```

## 비제어 Checkbox

자신의 상태를 스스로 관리하는 체크박스입니다.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function UncontrolledExample() {
  return (
    <Checkbox variant="default" defaultSelected={false}>
      뉴스레터 구독
    </Checkbox>
  );
}
```

## 제어 Checkbox

부모 컴포넌트에 의해 상태가 관리되는 체크박스입니다.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';
import { useState } from 'react';

export function ControlledExample() {
  const [isSelected, setIsSelected] = useState(false);

  return (
    <Checkbox
      variant="default"
      isSelected={isSelected}
      onChange={setIsSelected}
    >
      약관에 동의합니다
    </Checkbox>
  );
}
```

## 모든 Variant

사용 가능한 모든 시각적 변형을 보여줍니다.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function VariantsExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Checkbox variant="default">Default</Checkbox>
      <Checkbox variant="accent">Accent</Checkbox>
      <Checkbox variant="secondary">Secondary</Checkbox>
    </div>
  );
}
```

## 부정확한 상태

'전체 선택' 패턴을 위한 부정확한 상태입니다.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';
import { useState } from 'react';

export function IndeterminateExample() {
  const [items, setItems] = useState({
    item1: false,
    item2: true,
    item3: false,
  });

  const allSelected = Object.values(items).every(Boolean);
  const someSelected = Object.values(items).some(Boolean);

  const handleSelectAll = (isSelected: boolean) => {
    const newItems: typeof items = {};
    Object.keys(items).forEach(key => {
      newItems[key as keyof typeof items] = isSelected;
    });
    setItems(newItems);
  };

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Checkbox
        variant="default"
        isSelected={allSelected}
        isIndeterminate={someSelected && !allSelected}
        onChange={handleSelectAll}
      >
        모두 선택
      </Checkbox>
      {Object.entries(items).map(([key, value]) => (
        <Checkbox
          key={key}
          variant="default"
          isSelected={value}
          onChange={(checked) => setItems({ ...items, [key]: checked })}
        >
          {key}
        </Checkbox>
      ))}
    </div>
  );
}
```

## 비활성화 상태

비활성화된 체크박스를 보여줍니다.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function DisabledExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Checkbox variant="default" isDisabled>
        비활성화 (선택 안 함)
      </Checkbox>
      <Checkbox variant="default" isDisabled isSelected={true}>
        비활성화 (선택함)
      </Checkbox>
    </div>
  );
}
```
