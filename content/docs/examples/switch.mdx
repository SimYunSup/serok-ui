---
title: Switch 예시
description: 토글 스위치 컴포넌트의 다양한 사용 예시
---

## 기본 사용법

Switch 컴포넌트의 가장 간단한 사용 방법입니다.

```tsx
import { Switch } from '@/components/ui/Switch';

export function BasicExample() {
  return <Switch>알림 활성화</Switch>;
}
```

<PreviewWrapper>
  <Switch>알림 활성화</Switch>
</PreviewWrapper>

## 비제어 Switch

자신의 상태를 스스로 관리하는 스위치입니다.

```tsx
import { Switch } from '@/components/ui/Switch';

export function UncontrolledExample() {
  return (
    <Switch defaultSelected={false}>
      뉴스레터 구독
    </Switch>
  );
}
```

## 제어 Switch

부모 컴포넌트에 의해 상태가 관리되는 스위치입니다.

```tsx
import { Switch } from '@/components/ui/Switch';
import { useState } from 'react';

export function ControlledExample() {
  const [isSelected, setIsSelected] = useState(false);

  return (
    <Switch
      isSelected={isSelected}
      onChange={setIsSelected}
    >
      다크 모드
    </Switch>
  );
}
```

<PreviewWrapper>
  <ControlledSwitchExample />
</PreviewWrapper>

## 모든 변형

사용 가능한 모든 시각적 변형을 보여줍니다.

```tsx
import { Switch } from '@/components/ui/Switch';

export function VariantsExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Switch variant="default">기본</Switch>
      <Switch variant="accent">강조</Switch>
      <Switch variant="secondary">보조</Switch>
    </div>
  );
}
```

<PreviewWrapper>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
    <Switch variant="default">기본</Switch>
    <Switch variant="accent">강조</Switch>
    <Switch variant="secondary">보조</Switch>
  </div>
</PreviewWrapper>

## 비활성화 상태

비활성화된 스위치 상태를 보여줍니다.

```tsx
import { Switch } from '@/components/ui/Switch';

export function DisabledExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Switch isDisabled>비활성화 (꺼짐)</Switch>
      <Switch isDisabled isSelected={true}>
        비활성화 (켜짐)
      </Switch>
    </div>
  );
}
```

<PreviewWrapper>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
    <Switch isDisabled>비활성화 (꺼짐)</Switch>
    <Switch isDisabled isSelected={true}>
      비활성화 (켜짐)
    </Switch>
  </div>
</PreviewWrapper>

## 읽기 전용 Switch

읽기 전용 스위치 상태를 보여줍니다.

```tsx
import { Switch } from '@/components/ui/Switch';

export function ReadOnlyExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Switch isReadOnly>읽기 전용 (꺼짐)</Switch>
      <Switch isReadOnly isSelected={true}>읽기 전용 (켜짐)</Switch>
    </div>
  );
}
```
