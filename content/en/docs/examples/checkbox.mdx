---
title: Checkbox Example
description: Comprehensive Checkbox component examples with variants and states
---

# Checkbox Example

Complete guide to using the Checkbox component with various configurations.

## Basic Usage

The simplest way to use the Checkbox component.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function BasicExample() {
  return <Checkbox variant="default">Accept terms</Checkbox>;
}
```

<PreviewWrapper>
  <Checkbox variant="default">Accept terms</Checkbox>
</PreviewWrapper>

## Uncontrolled Checkbox

Checkbox managing its own state with a default initial value.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function UncontrolledExample() {
  return (
    <Checkbox variant="default" defaultSelected={false}>
      Subscribe to newsletter
    </Checkbox>
  );
}
```

## Controlled Checkbox

Checkbox with state managed by parent component.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';
import { useState } from 'react';

export function ControlledExample() {
  const [isSelected, setIsSelected] = useState(false);

  return (
    <Checkbox
      variant="default"
      isSelected={isSelected}
      onChange={setIsSelected}
    >
      I agree to the terms
    </Checkbox>
  );
}
```

## All Variants

Example showing all available visual variants.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function VariantsExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Checkbox variant="default">Default</Checkbox>
      <Checkbox variant="accent">Accent</Checkbox>
      <Checkbox variant="secondary">Secondary</Checkbox>
    </div>
  );
}
```

<PreviewWrapper>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
    <Checkbox variant="default">Default</Checkbox>
    <Checkbox variant="accent">Accent</Checkbox>
    <Checkbox variant="secondary">Secondary</Checkbox>
  </div>
</PreviewWrapper>

## Indeterminate State

Example showing indeterminate state for "select all" patterns.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';
import { useState } from 'react';

export function IndeterminateExample() {
  const [items, setItems] = useState({
    item1: false,
    item2: true,
    item3: false,
  });

  const allSelected = Object.values(items).every(Boolean);
  const someSelected = Object.values(items).some(Boolean);

  const handleSelectAll = (isSelected: boolean) => {
    const newItems: typeof items = {};
    Object.keys(items).forEach(key => {
      newItems[key as keyof typeof items] = isSelected;
    });
    setItems(newItems);
  };

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Checkbox
        variant="default"
        isSelected={allSelected}
        isIndeterminate={someSelected && !allSelected}
        onChange={handleSelectAll}
      >
        Select All Items
      </Checkbox>
      {Object.entries(items).map(([key, value]) => (
        <Checkbox
          key={key}
          variant="default"
          isSelected={value}
          onChange={(checked) => setItems({ ...items, [key]: checked })}
        >
          {key}
        </Checkbox>
      ))}
    </div>
  );
}
```

## Disabled State

Example showing disabled checkboxes.

```tsx
import { Checkbox } from '@/components/ui/Checkbox';

export function DisabledExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
      <Checkbox variant="default" isDisabled>
        Disabled (unchecked)
      </Checkbox>
      <Checkbox variant="default" isDisabled isSelected={true}>
        Disabled (checked)
      </Checkbox>
    </div>
  );
}
```
